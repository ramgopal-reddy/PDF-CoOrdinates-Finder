import React from 'react';

const Home: React.FC = () => {
    return (
        <div>
            <h1>Welcome to Home Page</h1>
            <p>This is a basic React + TypeScript + Vite setup.</p>
        </div>
    );
};

export default Home;